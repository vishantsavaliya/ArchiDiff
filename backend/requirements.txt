# Core dependencies for ArchiDiff
pillow==11.1.0
opencv-python==4.10.0.84
numpy==1.26.4
pymupdf==1.24.14

# EasyOCR for text detection and removal
easyocr
torch
torchvision

# MobileSAM for lightweight interactive segmentation (~40MB model, 5-7x faster than SAM)
mobile-sam @ git+https://github.com/ChaoningZhang/MobileSAM.git
matplotlib

# Flask for web interface
flask

